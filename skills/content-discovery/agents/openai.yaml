interface:
  display_name: "Content Discovery"
  short_description: "Generate high-signal route and file seeds for ShakerScan"
  default_prompt: "Use $content-discovery to build high-signal route, file, custom_list, and custom_endpoints seeds from ShakerScan evidence."
